import styled from "styled-components";
import { palette } from "../../constants/palette";

export const BigPlace = styled.div`
justify-content: center;
display: flex;
align-items: center;
background-color: ${() => palette.playGroundColor};
width: 300px;
height: 300px;
border-radius: 16px;

`